export {};
// TIP 1 : styled components y como extenderlos para cambiar su tipo de etiqueta y/o sus propiedades css. se puede cambiar el tipo de etiqueta cuando declaramos el styled component con la prop ' as="h2" '. o mas bien ' styled(Title).attrs({ as: h1 }) '.

// TIP 2 :
// :root{} sirve para declarar variables para toda la pagina.
// --gray-100: #c1c1c1. Para usar una escala de colores.
// color: var(--gray-100)

// TIP 3 :
// usar SWR para reemplazar state (repasar) como hook.
// api.ts (fetchAPI) y hooks.ts (useSWR)

// TIP 4 :
